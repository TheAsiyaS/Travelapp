import 'package:flutter/material.dart';

class UserSignInUpScrreen extends StatelessWidget {
  const UserSignInUpScrreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SignInup'),
      ),
      body: const SafeArea(
          child: Column(
        children: [],
      )),
    );
  }
}
